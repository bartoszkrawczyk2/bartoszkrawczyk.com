---
import { twMerge } from 'tailwind-merge';

interface Props {
  class?: string;
}

const { class: className, ...props } = Astro.props;
---

<div class={twMerge('max-w-6xl mx-auto px-4 sm:px-6 py-2', className)} {...props}>
  <slot />
</div>
